[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs = backups build dist .venv recovered_source .pytest-cache-label-removal-encoding .pytest-temp-label-removal-encoding
addopts = -p no:warnings --tb=short -q
